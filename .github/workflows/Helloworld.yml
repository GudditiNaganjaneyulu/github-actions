name: Print Branch Name

on:
  push:
    branches:
      - '*'

jobs:
  print-branch-name:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Print branch name
        run: |
          echo "Branch name: ${{ github.ref }}"
