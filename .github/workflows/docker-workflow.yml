name: docker build and push
on:
  push:
    branches:
      - main
        master
  pull_request:
    branches:
      - main
        master


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: docker version check 
        run: docker --version
