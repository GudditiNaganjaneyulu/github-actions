name: Hello World Workflow

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build and test
        run: |
          # Add your build and test commands here

      - name: Request approval
        id: approval
        uses: appleboy/approval@v0.0.1
        with:
          keyword: "true"

      - name: Send notification
        if: steps.approval.outputs.approved == 'true'
        run: |
          # Add your notification command here

      - name: Deploy
        if: steps.approval.outputs.approved == 'true'
        run: |
          # Add your deployment command here

  test:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Request test approval
        id: test_approval
        uses: appleboy/approval@v0.0.1
        with:
          keyword: "test"

      - name: Run tests
        if: steps.test_approval.outputs.approved == 'true'
        run: |
          # Add your test execution command here
